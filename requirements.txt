# NOTE: torch / torchvision are NOT listed here because CUDA sm_90 (Hopper /
# H100) requires the CUDA 12.1 wheels from PyTorch's own index. Install them
# first (see README "Setup"):
#   pip install torch==2.1.2 torchvision==0.16.2 \
#       --index-url https://download.pytorch.org/whl/cu121
#
# Everything below is installed afterwards with `pip install -r requirements.txt`.

ipython>=8.11
matplotlib>=3.7
numpy>=1.24,<2.0
opencv-python>=4.8
Pillow>=9.5
six>=1.16
scikit-image>=0.21
tensorboardX>=2.6
# required for --use_semantic_gate (frozen SegFormer-B0 Cityscapes backbone).
# Capped <4.38: newer transformers call torch.utils._pytree.register_pytree_node
# which only exists in torch>=2.2 (we pin torch 2.1.2 for the cu121 sm_90 build).
transformers>=4.33,<4.38
